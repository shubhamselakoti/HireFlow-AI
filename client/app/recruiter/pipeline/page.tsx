// Recruiter has full access to the application pipeline
// Re-export the same pipeline page — backend already allows hr_recruiter via isAdminOrRecruiter
export { default } from '@/app/dashboard/recruitment/applications/page';
